<template>
  <div aria-hidden="true" class="pointer-events-none fixed inset-0 z-0 overflow-hidden">
    <!-- Soft UI reads as one continuous surface, so the only backdrop
         left is a vignette that blends the page edges. -->
    <div class="orb-vignette absolute inset-0" />
  </div>
</template>
